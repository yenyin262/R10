import ScheduleContainer from "./ScheduleContainer";
import Schedule from "./Schedule";
export { Schedule }; // destructure if i want to get about without the about container component
export default ScheduleContainer;
