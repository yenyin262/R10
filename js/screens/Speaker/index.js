import SpeakerContainer from "./MapContainer";
import Speaker from "./Speaker";
export { Speaker }; // destructure if i want to get about without the about container component
export default SpeakerContainer;
