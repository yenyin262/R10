import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mapView: {
    flex: 1
  },
  mapText: {
    fontSize: 20,
    marginTop: 20,
    textAlign: "center"
  }
});

export default styles;
